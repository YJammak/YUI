<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:YUI.YProperties">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Colors.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="{x:Type ToggleButton}" x:Key="YToggleButtonDefaultStyle">
        <Setter Property="Foreground" Value="{StaticResource YButtonForeground}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="local:YAttachProperty.YMouseOverForeground" Value="{StaticResource YButtonMouseOverForeground}"/>
        <Setter Property="local:YAttachProperty.YPressedForeground" Value="{StaticResource YButtonMouseOverForeground}"/>
        <Setter Property="local:YAttachProperty.YCheckedForeground" Value="{StaticResource YButtonMouseOverForeground}"/>
        <Setter Property="local:YAttachProperty.YMouseOverBackground" Value="{StaticResource YButtonMouseOverBackground}"/>
        <Setter Property="local:YAttachProperty.YPressedBackground" Value="{StaticResource YButtonPressedBackground}"/>
        <Setter Property="local:YAttachProperty.YCheckedBackground" Value="{StaticResource YButtonPressedBackground}"/>
        <Setter Property="Background" Value="{StaticResource YButtonBackground}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Grid x:Name="Grid" Margin="{TemplateBinding Padding}">
                        <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Grid>
                    <!--z触发器-->
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" 
                                    Value="{Binding Path=(local:YAttachProperty.YMouseOverForeground), RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ToggleButton}}}"/>
                            <Setter Property="Background" 
                                    Value="{Binding Path=(local:YAttachProperty.YMouseOverBackground), RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ToggleButton}}}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Foreground" 
                                    Value="{Binding Path=(local:YAttachProperty.YPressedForeground), RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ToggleButton}}}"/>
                            <Setter Property="Background" 
                                    Value="{Binding Path=(local:YAttachProperty.YPressedBackground), RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ToggleButton}}}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Foreground" 
                                    Value="{Binding Path=(local:YAttachProperty.YCheckedForeground), RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ToggleButton}}}"/>
                            <Setter Property="Background" 
                                    Value="{Binding Path=(local:YAttachProperty.YCheckedBackground), RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ToggleButton}}}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Opacity" Value="{StaticResource YDisableOpacity}" TargetName="Grid"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ToggleButton}" x:Key="YToggleButtonIconStyle">
        <Setter Property="Foreground" Value="{StaticResource YButtonForeground}"/>
        <Setter Property="local:YAttachProperty.YMouseOverForeground" Value="{StaticResource YButtonForeground}"/>
        <Setter Property="local:YAttachProperty.YPressedForeground" Value="{StaticResource YButtonForeground}"/>
        <Setter Property="local:YAttachProperty.YCheckedForeground" Value="{StaticResource YButtonForeground}"/>
        <Setter Property="local:YAttachProperty.YIconSize" Value="20"/>
        <Setter Property="local:YAttachProperty.YIconMargin" Value="1"/>
        <Setter Property="local:YAttachProperty.YIcon" Value="&#xe664;"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Grid x:Name="Grid" Margin="{TemplateBinding Padding}">
                        <TextBlock x:Name="icon" 
                                   FontSize="{TemplateBinding local:YAttachProperty.YIconSize}"
                                   Text="{TemplateBinding local:YAttachProperty.YIcon}" 
                                   Foreground="{TemplateBinding Foreground}"
                                   Margin="{TemplateBinding local:YAttachProperty.YIconMargin}"
                                   FontFamily="{TemplateBinding local:YAttachProperty.YIconFontFamily}"
                       />
                    </Grid>
                    <!--z触发器-->
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" TargetName="icon"
                                    Value="{Binding Path=(local:YAttachProperty.YMouseOverForeground), RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ToggleButton}}}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Foreground"  TargetName="icon"
                                    Value="{Binding Path=(local:YAttachProperty.YPressedForeground), RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ToggleButton}}}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Foreground"  TargetName="icon"
                                    Value="{Binding Path=(local:YAttachProperty.YCheckedForeground), RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ToggleButton}}}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Opacity" Value="{StaticResource YDisableOpacity}" TargetName="Grid"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>